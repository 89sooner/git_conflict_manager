{
  "name": "@gsp/contract-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "echo 'lint contract-tests'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@apidevtools/swagger-parser": "^10.1.0",
    "@gsp/shared-types": "workspace:*",
    "@types/node": "^22.0.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "vitest": "^2.0.0",
    "yaml": "^2.5.0"
  }
}
